import React, { useState, useEffect } from 'react';
import { ScrollView, View, TextInput, TouchableWithoutFeedback, StyleSheet, Image, Alert, Pressable,View,Text } from 'react-native';

const Form = (props)=>{
    return (
        <Text>
        123
        </Text>
    )
}

export default Form;